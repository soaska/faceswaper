services:
  face-swap-api:
    build:
      context: .
      dockerfile: Dockerfile-cpu
      target: base
    environment:
      - DEVICE_TYPE=cpu
    restart: unless-stopped
    ports:
      - "7860:7860"
    volumes:
      - ./data:/app/temp 